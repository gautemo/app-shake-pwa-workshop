{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "netlify:init": "netlify login && netlify sites:create --name app-shake && netlify link --name app-shake",
    "netlify:deploy": "npm run build && netlify deploy --prod --dir dist"
  },
  "dependencies": {
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react-refresh": "^1.3.1",
    "netlify-cli": "^3.35.1",
    "typescript": "^4.1.2",
    "vite": "^2.3.5"
  }
}
